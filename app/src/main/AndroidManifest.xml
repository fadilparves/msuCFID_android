<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fadil.msucfid">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="false"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CourseActivity"/>
        <activity android:name=".ContentDisplay" />
        <activity android:name=".InterActivity" />
        <activity android:name=".VideoListActivity"/>
        <activity android:name=".VideoActivity"/>
        <activity android:name=".NewPersonalNote" android:noHistory="true"/>
        <activity android:name=".PersonalNotesListActivity"/>
        <activity android:name=".ShowPersonalNote"/>
        <activity android:name=".UpdatePersonalNotes" android:noHistory="true"/>
    </application>

</manifest>